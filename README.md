# QuizRush AI Premium

Render:
- Build Command: `npm install`
- Start Command: `npm start`
- Env: `GROQ_API_KEY=...`

Firebase:
- Email/Password ve Anonymous aç
- Authorized Domains içine Render domainini ekle
- Test rules: read/write true

Sayfalar: /, /teacher, /student, /host, /lobby, /game, /results, /account, /debug, /mobile
